<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>New Project Created</title>
    <style>
      body { font-family: Arial, sans-serif; color: #333; } .container {
      max-width: 600px; margin: auto; padding: 24px; border: 1px solid #eee;
      border-radius: 8px; } .logo { text-align: center; margin-bottom: 20px; }
      .logo img { width: 120px; } .highlight { color:
      {{primaryColor}}; font-weight: bold; } .footer { font-size: 12px; color:
      #888; margin-top: 30px; text-align: center; } .btn { display:
      inline-block; padding: 10px 20px; margin-top: 20px; background-color:
      {{primaryColor}}; color: white; text-decoration: none; border-radius: 4px;
      } ul { padding-left: 20px; } li { margin-bottom: 4px; }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="logo">
        <img src="{{logoUrl}}" alt="Scopology Logo" />
      </div>

      <p>Hi {{adminName}},</p>

      <p>
        A new project titled
        <strong>"{{projectName}}"</strong>
        has just been
        <span class="highlight">created</span>
        in Scopology.
      </p>

      <p><strong>Created By:</strong> {{createdBy}}</p>

      <p><strong>Description:</strong></p>
      <p>{{description}}</p>

      <a href="{{projectLink}}" class="btn" target="_blank">View Project</a>

      <p>Thanks,<br />The Scopology Team</p>

      <div class="footer">
        &copy;
        {{year}}
        Scopology. All rights reserved.
      </div>
    </div>
  </body>
</html>