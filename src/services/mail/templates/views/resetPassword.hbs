<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Reset Your Password</title>
    <style>
      body { font-family: Arial, sans-serif; color: #333; } .container {
      max-width: 600px; margin: auto; padding: 24px; border: 1px solid #eee;
      border-radius: 8px; } .logo { text-align: center; margin-bottom: 20px; }
      .logo img { width: 120px; } .highlight { color:
      {{primaryColor}}; font-weight: bold; } .btn { display: inline-block;
      background:
      {{primaryColor}}; color: #fff; padding: 10px 16px; text-decoration: none;
      border-radius: 4px; margin-top: 16px; } .footer { font-size: 12px; color:
      #888; margin-top: 30px; text-align: center; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="logo">
        <img src="{{logoUrl}}" alt="Scopology Logo" />
      </div>

      <p>Hi {{fullName}},</p>
      <p>You recently requested to reset your password.</p>
      <p>Click the button below to reset it:</p>

      <p><a class="btn" href="{{resetLink}}">Reset Password</a></p>

      <p>This link will expire in 10 minutes. If you didn’t request a reset, you
        can safely ignore this email.</p>

      <p>Regards,<br />The Scopology Team</p>

      <div class="footer">
        &copy;
        {{year}}
        Scopology. All rights reserved.
      </div>
    </div>
  </body>
</html>